import React from "react";
import Form from "../components/CreateNoteForm/Form";

const CreateNote = () => {
  return (
    <div>
      <h1>Create Note</h1>
      <Form />
    </div>
  );
};

export default CreateNote;
